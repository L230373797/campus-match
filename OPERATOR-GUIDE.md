# 校园匹配运营后台使用说明

最后更新：2026-04-28

后台地址：

```text
https://campus-match-sansui.netlify.app/admin.html
```

## 1. 后台能做什么

运营后台目前负责两类工作：

- 认证队列：处理用户上传校园卡后的校园认证申请。
- 账号资料请求：处理用户提交的清空资料、注销账号等请求。

后台不会替用户聊天、匹配或修改普通资料。遇到用户资料填写错误，优先让用户回到“我的资料”自行修改。

## 2. 登录和权限

使用有后台权限的账号登录。

后台权限来源有两种：

- 账号邮箱被配置在 `CAMPUS_ADMIN_EMAILS` 环境变量里。
- 用户数据记录里 `isAdmin` 为 `true`。

如果登录后提示“当前账号没有后台权限”，说明账号能登录用户端，但还没有运营后台权限。

建议：

- 后台账号单独使用，不和普通测试账号混用。
- 密码至少 8 位，并避免和 QQ 邮箱密码相同。
- 不把后台账号发给不参与运营的人。
- 离开电脑前点“退出后台”。

## 3. 认证队列怎么处理

进入后台后默认打开“认证队列”。

每条认证申请会显示：

- 昵称
- 学校、专业、年级
- 提交时间
- 邮箱、学号、学院、宿舍或校区
- 校园卡照片
- 处理记录输入框

处理步骤：

1. 点“查看校园卡原图”。
2. 确认照片能证明用户属于所填学校。
3. 核对学校、学号、学院等信息是否明显矛盾。
4. 在“处理记录”里填写一句简短说明。
5. 符合条件点“通过认证”，不符合点“驳回申请”。

通过认证后：

- 用户资料页会显示校园认证状态。
- 用户在推荐和资料展示里更容易获得信任。

驳回申请后：

- 用户端会看到处理记录。
- 用户可以补充资料或重新上传校园卡后再提交。

常用处理记录：

```text
认证通过，校园信息已核验。
照片信息不够清晰，请重新上传能看清学校信息的照片。
学校信息和资料填写不一致，请修改资料后重新提交。
图片无法打开或格式异常，请重新上传。
```

## 4. 账号资料请求怎么处理

点击“账号资料请求”标签查看待处理请求。

目前请求类型：

- 清空资料：用户希望清理个人资料，但保留账号。
- 注销账号：用户希望账号不可再登录，并清理/匿名化相关资料。

每条请求会显示：

- 用户昵称和邮箱
- 学校、专业、年级
- 提交时间
- 请求类型
- 用户填写的说明
- 处理记录输入框

处理步骤：

1. 阅读请求类型和用户说明。
2. 判断是否可以处理。
3. 在“处理记录”里填写处理结果。
4. 可处理时点“确认完成”。
5. 不能处理或信息不清楚时点“驳回请求”，并写清楚原因。

重要提醒：

- “确认完成”可能会清空资料或注销账号。
- 注销账号完成后，用户不能再登录这个账号。
- 对明显误操作、说明不清楚、疑似非本人操作的请求，先驳回并让用户重新确认。
- 用户在待处理期间可以自行撤回请求，撤回后不会继续出现在待处理队列里。

常用处理记录：

```text
已按申请完成资料清理。
已按申请完成账号注销。
说明不够明确，请重新提交并写清楚需要处理的内容。
为避免误操作，本次请求已驳回；如仍需处理，请重新提交。
```

## 5. 日常运营流程

每天建议按这个顺序处理：

1. 打开运营后台。
2. 刷新认证队列。
3. 处理所有清晰、可信的认证申请。
4. 对资料不完整或图片不清楚的申请写明原因并驳回。
5. 切到账号资料请求。
6. 先处理清空资料，再谨慎处理注销账号。
7. 处理完后再刷新一次队列，确认没有遗漏。
8. 退出后台。

## 6. 什么情况不要通过

认证申请不要通过：

- 校园卡照片看不清学校信息。
- 用户填写学校和照片学校明显不一致。
- 图片疑似截图拼接、网图或他人证件。
- 学号、学院、专业等信息明显异常。
- 用户说明要求跳过认证或手动开权限。

账号资料请求不要直接完成：

- 请求说明看起来像误点。
- 用户只写了“试试”“看看”之类不明确内容。
- 注销账号请求里有明显矛盾。
- 你不确定完成后会造成什么影响。

## 7. 常见问题

### 登录后进不去后台

原因通常是账号没有后台权限。检查：

- 账号邮箱是否在 `CAMPUS_ADMIN_EMAILS`。
- 用户记录里 `isAdmin` 是否为 `true`。
- 是否登录错账号。

### 队列为空

可能是当前没有待处理申请。可以：

- 点“刷新队列”。
- 让测试用户重新上传校园卡。
- 让测试用户重新提交账号资料请求。

### 校园卡图片打不开

先不要通过认证。可以驳回并填写：

```text
图片无法打开，请重新上传校园卡照片。
```

### 用户说验证码收不到

先确认：

- 是否使用 QQ 邮箱。
- 邮箱地址有没有输错。
- 垃圾邮件里有没有验证码。
- QQ SMTP 服务是否正常。

### 用户误提交注销账号

如果还在待处理状态，用户可以在“我的资料”里撤回申请。运营侧也可以驳回请求，并备注：

```text
为避免误操作，本次注销请求已驳回；如仍需注销，请重新提交。
```

## 8. 上线前后台检查

上线前建议运行：

```bash
npm run check:launch
```

并人工确认：

- 后台账号能登录。
- 认证队列能刷新。
- 账号资料请求能刷新。
- 通过认证、驳回认证、驳回账号资料请求的按钮可用。
- “确认完成”注销类请求前会弹出确认框。

## 9. 数据和安全注意

- 不下载、不转发用户校园卡照片。
- 不在群聊里公开用户邮箱、学号、宿舍等信息。
- 处理记录要简短、客观，不写情绪化内容。
- 删除或注销类请求要谨慎，能驳回确认就先驳回确认。
- 正式运营前要建立数据备份和管理员账号变更记录。
